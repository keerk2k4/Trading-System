# Tells the grading harness where to find the package and each function,
# since every team names things differently. This is the "Engineering Contract".

PACKAGE_NAME=sprint-04-analytics-etl
PACKAGE_MODULE=analytics_etl

EXTRACT_FUNC=analytics_etl.extract:extract
TRANSFORM_FUNC=analytics_etl.transform:transform
LOAD_FUNC=analytics_etl.load:load

MALFORMED_INPUT_TEST=tests/test_transform.py::test_transform_rejects_malformed_input

ENTRY_POINT=analytics-etl
